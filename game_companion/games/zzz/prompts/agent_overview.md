This is a Zenless Zone Zero Agent overview/attribute screen.
Extract into these JSON keys:
- character_name: the Agent's name
- level: the Agent level
- duplication_level: the Mindscape rank shown on the screen
- attribute, specialty, faction if visible.
Use null for anything not visible or unreadable. Never guess.
Output only JSON matching the schema.
