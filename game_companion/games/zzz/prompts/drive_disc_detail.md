This is a Zenless Zone Zero Drive Disc detail screen showing one disc with its
main stat and up to 4 substats. Extract slot, set name, rarity, level, main
stat (key and value), and each substat (key, value, number of rolls if shown).
Use null for anything unreadable. Never guess values. Output only JSON
matching the schema.
